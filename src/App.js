import ExpenseItem from "./components/ExpenseItem";

function App() {
  // ! JSX (Javascript XML) allow us to write HTML code in Js files
  return (
    <div>
      <h2>Let's get started!</h2>
      <ExpenseItem></ExpenseItem>
    </div>
  );
}

export default App;
